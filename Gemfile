source 'https://rubygems.org'

# Gems for running external commands
gem 'open3'

# Open AI
gem 'ruby-openai'

# Gem for generating reports in markdown
gem 'kramdown'

# Gem for handling HTTP requests (e.g., for fetching data if needed)
gem 'httparty'

# Gems for testing
gem 'rspec'
gem 'rake'

# Gem for handling YAML configuration
gem 'yaml'

# Gem for loading environment variables
gem 'dotenv'

# Gem for checking target reachability
gem 'net-ping'
